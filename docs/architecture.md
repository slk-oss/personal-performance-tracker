## TODO по модели Log:

1. Дата — timestamp: datetime с дефолтом "сейчас", без валидации "не в будущем" (это добавлю позже в schemas/, когда дойду до роутера /log).

2. Status — bool без дефолта в модели (обязательное поле), а дефолт на уровне API-схемы позже.

3. Уникальность — пока без UniqueConstraint, проверку дублей позже.